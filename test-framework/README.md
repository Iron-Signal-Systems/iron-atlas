# Test Framework

Run all currently executable checks:

```bash
./test-framework/run_all.sh
```

Results are written to `test-framework/test-results/`. Successful and failed correctness outcomes remain separate from future resource observations.
